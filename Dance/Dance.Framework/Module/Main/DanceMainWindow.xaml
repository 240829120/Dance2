<dx:ThemedWindow x:Class="Dance.Framework.DanceMainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:dance="http://schemas.dance.com/wpf"
                 xmlns:local="clr-namespace:Dance.Framework"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
                 xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
                 xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                 xmlns:dxdove="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements"
                 WindowStartupLocation="CenterScreen"
                 mc:Ignorable="d" Background="Transparent" x:Name="uc" Height="1000" Width="1600"    
                 d:DataContext="{d:DesignInstance Type=local:DanceMainWindowModel}"
                 dance:DanceFrameworkElementHelper.LoadedOnceCommand="{Binding Path=LoadedCommand}">
    <dx:ThemedWindow.Resources>
        <DataTemplate x:Key="BarTemplate">
            <ContentControl>
                <dxb:Bar Caption="{Binding Content}" ItemLinksSource="{Binding Items}" ShowDragWidget="True" AllowHide="False"></dxb:Bar>
            </ContentControl>
        </DataTemplate>

        <DataTemplate x:Key="BarTemplate_Status">
            <ContentControl>
                <dxb:Bar Caption="{Binding Content}" ItemLinksSource="{Binding Items}" ShowDragWidget="False" AllowHide="False" IsStatusBar="True"/>
            </ContentControl>
        </DataTemplate>
    </dx:ThemedWindow.Resources>

    <dx:ThemedWindow.ToolbarItems>
        <dxb:MainMenuControl VerticalAlignment="Center"/>
    </dx:ThemedWindow.ToolbarItems>

    <dxb:BarManager BarsSource="{Binding Bars}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <!-- 通知 -->
            <local:DanceMessageNotification></local:DanceMessageNotification>
            <!-- Docking -->
            <dxdo:DockLayoutManager x:Name="PART_DockLayoutManager" ClosedPanelsBarVisibility="Manual">
                <dxdo:LayoutGroup DestroyOnClosingChildren="False">
                    <dxdo:DocumentGroup ItemsSource="{Binding Path=Documents}" DestroyOnClosingChildren="False"  ClosePageButtonShowMode="InAllTabPageHeaders"></dxdo:DocumentGroup>
                    <dxdo:LayoutGroup ItemsSource="{Binding Path=Layouts}" DestroyOnClosingChildren="False"></dxdo:LayoutGroup>
                </dxdo:LayoutGroup>
            </dxdo:DockLayoutManager>
            <!-- 状态 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <dxb:StatusBarControl HideWhenEmpty="False" AllowCustomizationMenu="False"
                                      ItemsSource="{Binding StatusBarLeftItems}"></dxb:StatusBarControl>
                <dxb:StatusBarControl HideWhenEmpty="False" AllowCustomizationMenu="False" Grid.Column="1"
                                      ItemsSource="{Binding StatusBarRightItems}"></dxb:StatusBarControl>
            </Grid>
        </Grid>
    </dxb:BarManager>


</dx:ThemedWindow>
